<div id="featured">

  <% unless Game.first.nil? %>
    <%= div_for(Game.first) do |g| %>
      <%= link_to play_game_url(id: g.path) do %>
        <%= image_tag g.image.url ,class: "game-img" %>
      <% end %>

      <span>Play Now</span>
    <% end %>
  <% end %>
</div>