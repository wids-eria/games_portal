<%= render :partial => 'forem/forums/head', :locals => { :forum => @topic.forum } %>

<h2 class="form_header"><%= t("forem.topic.new") %></h2>

<% flash[:notice] = error_format(@topic.errors)%>
<%= render "form" %>
