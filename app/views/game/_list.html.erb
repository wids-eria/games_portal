<h3>Games</h3>
<div id="games">
  <%= div_for(games, :class => "game") do |g| %>
    <%= link_to game_landing_url(game: g.path) do %>
     <div class="placeholder">



     </div>
    <% end %>

    <div class="description">
      <h1><%= g.name %></h1>
      <p><%= g.description %></p>
    </div>
  <% end %>
</div>